version: '3.8'
networks:
  ubrato:
    driver: bridge

services:
  api:
    container_name: ubrato-api
    image: registry.ubrato.ru:5050/godmod/ubrato-back:latest
    expose:
      - "3000"
    ports:
      - "3000:3000"
    networks:
      - ubrato
    depends_on:
      - db
    restart: "on-failure"

  db:
    container_name: ubrato-db
    image: postgres
    expose:
      - "5432"
    volumes:
      - "./app/repositories/migration/:/docker-entrypoint-initdb.d/"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "12345"
      POSTGRES_DB: "postgres"
    networks: 
     - ubrato
    restart: "on-failure"